<user>
T2I 이미지 생성 모델의 성능을 측정하기 위한 메트릭은 뭐뭐가 있는지 큰 범주로 먼저 그룹화 한 다음 각각에 속한 메트릭에는 뭐가 있는지 정리해줘.

일단 프롬프트가 설명한 이미지를 잘 생성해야 하고, 특정 스타일을 갖도록 프롬프팅을 할건데(다른 대화에서 나왔던 교육용 이미지) 이 스타일을 잘 반영할 수록 높은 점수가 나오는 메트릭이 뭔지 알고싶어

아니면 그냥 vlm을 쓰는 추세인가? 아니면 채점 모델을 따로 학습해서 사용하나? 그런 최신 연구에 대한 전반적인 조사도 함께 진행해서 정리해서 알려줘
</user>
<claude>
T2I 평가 메트릭은 최근 2~3년 사이에 많이 바뀐 영역이라, 최신 연구 동향을 확인하고 정리해줄게. 검색부터 할게.좋은 서베이 자료들을 찾았어. 정렬 메트릭과 스타일 관련 부분을 더 파봐야겠어.정렬 메트릭 쪽은 충분히 모았어. 이제 스타일 반영도를 측정하는 메트릭 쪽을 파볼게 — 이게 교육용 스타일 관련해서 핵심이니까.스타일 메트릭도 잘 정리됐어. 마지막으로 VLM-as-judge 추세와 채점 모델 학습 쪽을 확인할게.T2I 평가는 최근 2~3년간 "FID + CLIPScore" 시절에서 완전히 재편됐어. 큰 범주부터 나누고, 그다음 스타일 얘기랑 최신 추세를 정리할게.

## 1. 대범주별 메트릭 정리

### (A) 분포·이미지 품질 — 프롬프트 무관, 이미지 자체의 리얼리즘/품질
- **FID / IS / KID**: 생성 이미지 분포와 실제 이미지 분포 간 거리. IS와 FID는 전통적 화질 지표지만 텍스트-이미지 정렬은 측정하지 못하고, 현대 모델의 풍부한 콘텐츠를 잘 못 담는다는 비판이 있어.
- **FID 개선판**: CMMD(FID-DINOv2), sFID 등. Inception 백본 대신 더 나은 임베딩 사용.
- **No-reference IQA**: BRISQUE, NIQE, CLIP-IQA, Q-Align — 참조 없이 화질/왜곡만 평가.
- **미학 예측기**: LAION aesthetic predictor 등.

### (B) 텍스트-이미지 정렬(faithfulness) — 프롬프트대로 그렸나
- **임베딩 기반**: CLIPScore, BLIPScore. 가장 단순하지만 CLIP 유사도는 인간 선호를 반영하지 못한다는 한계가 있어.
- **VQA/QG-QA 기반** (지금 정렬 평가의 주류): 프롬프트를 질문으로 분해해 VQA 모델에 물어보고 정답률로 채점.
  - **TIFA**: LLM으로 질문-답 쌍을 생성하고, VQA 모델이 그 질문에 답할 수 있는지로 충실도를 계산.
  - **DSG (Davidsonian Scene Graph)**: 텍스트를 씬 그래프로 변환해 개체와 관계를 검증. 의존성 DAG로 논리적으로 무효한 채점(대상이 없는데 "빨간가?"를 묻는 등)을 방지.
  - **VQAScore**: 이미지가 텍스트와 맞는지 VLM에게 물어 "Yes"라고 답할 확률을 계산하는 종단간 정렬 지표. TIFA/DSG와 달리 복잡한 프롬프트 분해가 필요 없고 VLM의 전체적 멀티모달 추론에 의존.
  - **변형들**: DA-Score(속성 바인딩), B-VQA(BLIP-VQA로 객체-속성쌍 분해), Soft-TIFA(하드 정답 대신 VQA 소프트 확률).
- **벤치마크와 결합**: GenEval(객체 수·위치·색), T2I-CompBench(컴포지션), DPG-Bench(dense prompt).

### (C) 인간 선호 보상 모델 — 사람이 좋아하는가
- HPS v2, ImageReward, PickScore, MPS, Social Reward 등은 인간 선호 데이터에 강화학습으로 파인튜닝된 평가 지표. 최근엔 HPSv3까지.
- 이건 정렬+화질+미감을 뭉뚱그린 "전반적 좋음" 신호라 학습(RLHF/보상) 용도로도 많이 씀.

### (D) 스타일·참조 유사도 — **네가 원하는 부분**
아래 2번에서 자세히.

### (E) 지식·상식 계열 (참고)
WISE, MMMG, Commonsense-T2I 등 — 프롬프트에 함축된 세계 지식을 그렸나 평가. 네 용도랑은 거리가 있어.

---

## 2. "스타일 반영도"를 재는 메트릭

여기가 핵심인데, 중요한 함정이 하나 있어. **CLIP-I(CLIP 이미지 유사도)는 스타일 측정에 부적합**해:

> CLIP은 클래스 레이블로 학습돼서 콘텐츠가 비슷하면 스타일이 달라도 높은 유사도를 준다. 반면 DINO 임베딩은 self-supervised라 스타일 변화에 더 민감해서 스타일 정렬 평가에 더 적합하다.

그래서 스타일 정량화 후보는 이렇게 정리돼:

- **DINO / DINOv2 코사인 유사도**: 생성물 vs 참조 스타일 이미지의 임베딩 유사도. 스타일 정렬 평가의 사실상 표준 베이스라인. 한 세트 내 생성 이미지들의 DINO ViT-B/8 임베딩 쌍별 평균 코사인 유사도로 스타일 일관성을 측정하는 식.
- **CSD (Contrastive Style Descriptor)**: **스타일 전용으로 학습된 지표**라 가장 직접적. CSD는 참조 스타일에 대한 예술적 특징 유사도를 직접 정량화해. 같은 스타일이면 높게, 다른 스타일이면 낮게 나오도록 설계됨.
- **DreamSim / DiffSim**: 인간 지각에 맞춘 구조·스타일 유사도. DiffSim은 diffusion 모델의 self-attention으로 스타일 유사도를 재고, 사람이 고른 508개 스타일 벤치마크로 검증.
- **Gram matrix (style loss 계열)**: 첫 프레임과 마지막 프레임의 Gram matrix 차이(F-norm)로 스타일 일관성을 평가. 전통적 style transfer의 텍스처 통계 방식.

**단, DINO 계열의 함정**: content leakage 문제가 있어. DINO 임베딩 코사인 유사도는 참조 이미지의 의미적 콘텐츠 누출에 의해 점수가 올라가서, 스타일뿐 아니라 의미·구조 일관성까지 같이 높여버리는 게 스타일 정렬 관점에선 바람직하지 않다. 즉 "같은 곰"을 그리면 스타일 무관하게 점수가 오를 수 있음. 그래서 CSD가 더 순수한 스타일 신호를 준다고 봐.

### 네 교육용(굵고 균일한 선 + 플랫 컬러) 케이스에 대한 현실적 조언
스타일이 "특정 화풍"이라기보다 **구체적 시각 속성**(선 두께 균일도, 플랫한 색면, 채색 여부)에 가까워. 이런 건:
- **참조 이미지 셋이 있으면** → CSD 또는 DINOv2 유사도로 정량 점수를 뽑는 게 표준.
- **속성이 명시적으로 정의되면** → VLM judge에 rubric("선 두께가 균일한가? 색이 그라데이션 없이 플랫한가? 명암/그림자가 없는가?")을 주고 채점시키는 게 요즘 방식이고, 콜링북 특유의 "굵고 균일한 외곽선"은 아예 전통 CV(edge detection → 선 두께 분산)로 커스텀 지표를 만드는 게 CSD보다 더 정확하고 해석 가능할 수 있어. 이건 단일 용도라 과하게 추상화하지 말고 간단히 짜는 걸 추천.

---

## 3. 최신 추세: VLM-as-judge냐, 전용 채점 모델 학습이냐

결론부터: **둘 다 진행 중이고, VLM/MLLM-as-judge로 무게중심이 넘어왔어.** 대략 세 갈래로 갈려.

**(1) Zero-shot으로 강한 closed VLM을 직접 judge로**
GPT-4o, Gemini를 그대로 채점자로 씀. 강력한 MLLM의 등장으로 "MLLM-as-a-Judge" 패러다임이 자리잡았고 인간 판단과 높은 상관을 보이면서, 수동적 평가에서 생성 모델을 최적화하는 능동적 보상으로 쓰는 쪽으로 이동했어. MJ-Bench 결과 closed-source VLM이 대체로 더 나은 피드백을 주고 GPT-4o가 평균적으로 최고.

**(2) 전용 evaluator/reward 모델을 파인튜닝**
UnifiedReward, LLaVA-Critic, Prometheus-Vision, HPSv3 등. Prometheus-V가 MLLM으로 전용 멀티모달 평가 모델을 처음 구축했고, LLaVA-Critic이 GPT가 주석한 pairwise/pointwise 데이터로 더 큰 평가 모델을 학습. 심지어 인간 주석 없이 합성 선호 데이터로 self-improving하는 VLM judge가 Llama-3.2-11B를 반복 학습해 훨씬 큰 90B 모델이나 Claude 3.5 Sonnet을 능가하기도 해.

**(3) Rubric 기반 채점 (떠오르는 중)**
프롬프트별로 채점 기준(rubric)을 생성해서 규칙 기반으로 채점. AutoRubric-T2I 같은 게 여기 속하고, VLM judge의 불안정성을 보완하려는 흐름이야.

**어떤 게 언제 유리한가** (MJ-Bench의 실증):
> 작은 scoring 모델(CLIP 기반)은 텍스트-이미지 정렬과 화질 피드백에서 더 낫고, VLM은 강한 추론 능력 덕에 안전성·생성 편향 피드백에서 더 정확하다.

그리고 정렬 평가 쪽에선 VQA 소프트 확률 방식이 강세야. Soft-TIFA는 Qwen3-VL-8B 백본으로 94.5% AUROC를 달성해 VQAScore(92.4%)와 TIFA(91.6%)를 인간 정렬 프로토콜에서 능가했어.

---

### 정리하면 — 네 파이프라인에 실용적으로 붙일 스택
- **프롬프트 충실도**: VQAScore 또는 Soft-TIFA(Qwen-VL 백본). 분해 필요 없는 VQAScore가 세팅이 제일 간단.
- **스타일 반영도**: 참조 셋 대비 CSD(우선) 또는 DINOv2 유사도 + 콜링북 고유 속성은 VLM rubric 채점이나 커스텀 CV 지표.
- **화질/미감**: no-ref IQA + aesthetic predictor.
- **전체 통합 판단이 필요하면**: GPT-4o/Gemini에 rubric 줘서 pointwise 채점 (단, pointwise는 pairwise보다 불안정한 경향).

참조 이미지 셋 유무랑, 스타일을 "화풍 매칭"으로 볼지 "속성 만족"으로 볼지에 따라 지표 선택이 갈리는데 — 어느 쪽으로 잡을 생각이야? 그거에 맞춰서 CSD 셋업이든 rubric 설계든 더 구체적으로 도와줄게.
</claude>
<user>
프롬프트 충실도랑 스타일 반영도 두 가지가 중요해
</user>
<claude>
두 축을 다 잡을 거면, 각 축의 대표 지표를 하나씩 확정하고 둘 사이의 트레이드오프를 관리하는 게 핵심이야.

## 프롬프트 충실도 → VQAScore 하나로 시작
분해가 필요 없는 종단간 지표라 세팅이 제일 간단하고, 3090에서 충분히 돌아가. 이미지-프롬프트가 맞는지 VLM에게 물어 "Yes" 확률을 점수로 쓰는 방식이라 파이프라인에 붙이기 좋아. 더 세밀한 per-atom 분석(객체/속성/관계별로 어디서 틀렸나)이 필요해지면 그때 Soft-TIFA(Qwen-VL 백본)로 올리면 돼. 처음부터 TIFA/DSG 같은 분해형으로 갈 필요는 없어 — 오버킬이야.

## 스타일 반영도 → 정의 방식에 따라 갈림
이건 스타일을 뭘로 정의하느냐가 지표를 결정해서, 아래에서 물어볼게.

## ⚠️ 두 축은 서로 상충한다 (이게 제일 중요)
관련 논문들이 **text-alignment vs style-consistency를 대놓고 트레이드오프 축으로 그려.** 스타일을 강하게 밀면(스타일 프롬프트 과하게 / LoRA 세게) 충실도가 떨어지고, 반대도 마찬가지야. 그래서:

- **개발 중에는 두 점수를 합치지 말고 따로 트래킹**해. Streamlit 리뷰 앱에 두 축을 2D 산점도로 찍으면 "스타일은 좋은데 프롬프트 무시하는 버전" vs "프롬프트는 맞는데 밋밋한 버전"이 바로 보여.
- **버전 랭킹용 단일 숫자가 필요하면** 두 점수의 조화평균(논문들의 `S_dual = 2·S_obj·S_sty/(S_obj+S_sty)` 방식)을 쓰면 한쪽만 높은 버전에 페널티가 걸려서 균형 잡힌 버전이 위로 올라와.

이렇게 하면 네 filesystem 버저닝 워크플로에 "충실도 / 스타일 / 조화평균" 세 컬럼으로 깔끔하게 붙어.

스타일 지표를 확정하려면 하나만 정해줘 — 콜링북 스타일을 어떻게 정의할 생각이야?
</claude>
<user>
질문: 콜링북 스타일을 어떻게 정의할 거야? (스타일 지표 선택이 여기서 갈려)
답: 둘 다 활용 가능
</user>
<claude>
좋아, 둘 다 되면 스타일을 **두 서브지표로 쪼개는 게 정석**이야. 화풍 매칭과 콜링북 고유 구조는 서로 다른 걸 재거든.

## 스타일 = CSD(화풍) + 커스텀 CV(콜링북 속성)

**① CSD — 참조 셋 대비 화풍 유사도**
네 레퍼런스 콜링북 이미지 셋과 생성물의 CSD 점수. "전반적으로 그 화풍인가"를 재는 주 신호. 스타일 전용으로 학습된 지표라 DINO보다 content leakage에 덜 휘둘려서, 같은 대상을 그려도 스타일만 순수하게 비교돼. 3090에서 가볍게 돌아가.

**② 커스텀 CV — "굵고 균일한 선 + 플랫 컬러"를 직접 측정**
이게 CSD가 놓치는 부분이야. CSD는 "대충 그 느낌"은 잡지만 선이 얇아지거나 그라데이션이 새는 건 못 짚어. 콜링북은 속성이 명확하니까 결정론적으로 재는 게 더 정확하고 해석 가능해:

- **선 두께 균일도**: 외곽선 이진화 → skeletonize → distance transform로 각 선의 두께 측정 → 변동계수(std/mean). 낮을수록 균일. 평균 두께로 "굵기"도 같이 나와.
- **컬러 플랫니스**: 엣지 마스크 바깥 영역의 Sobel 그래디언트 크기 평균. 플랫하면 색면 내부 그래디언트가 0에 가까움. 여기에 색 양자화 후 distinct color 수를 더하면 "단순한 색면인가"도 잡혀.

이 둘은 GPU도 안 쓰고 밀리초 단위라 리뷰 앱에 붙여도 부담 없고, 값이 왜 낮은지 바로 설명돼서 프롬프트 튜닝 피드백으로 직결돼.

**③ VLM rubric — 선택**
CV로 못 잡는 미묘한 것(예: "아이 친화적 단순함", "명암 없음")만 Qwen2.5-VL-7B에 rubric 줘서 보조로. 처음부터 넣진 말고, ①+②로 부족할 때만 추가해. 오버킬 피하는 쪽으로.

## 최종 롤업 (Streamlit 리뷰 앱 컬럼)
```
충실도:   VQAScore
스타일:   CSD × 콜링북속성점수(선균일도·플랫니스 정규화)
종합:     조화평균(충실도, 스타일)   ← 버전 랭킹용
```
개발 중엔 충실도·스타일 2D 산점도로 트레이드오프 보고, 배포/자동랭킹은 조화평균 한 컬럼으로.

---

이거 camp-16 서버에 바로 붙일 수 있게 스코어링 모듈로 짜줄 수 있어. CSD 로더 + 커스텀 CV 지표 + VQAScore 래퍼를 하나의 함수(`score_image(image, prompt, ref_set) → dict`)로 묶어서 네 filesystem 버저닝/Streamlit에 꽂는 형태. 

짜기 전에 두 가지만 확인할게: 콜링북 출력이 **채색된 플랫 일러스트**야, 아니면 **흑백 라인아트**야? (컬러 플랫니스 지표 계산이 갈려) 그리고 CSD는 공식 구현체 그대로 쓸 생각이야, 아니면 이미 서버에 뭔가 깔아둔 게 있어?
</claude>
<user>
출력 : 채색된 플랫 일러스트, CSD를 쓰고 싶은데, 아직 정답 이미지가 없어서 정답 이미지도 검색해서 가져오고 싶은데, 어떤 키워드로 검색하면 좋을까?
</user>
<claude>
CSD 참조 셋을 만들기 전에, 검색 키워드보다 먼저 짚어야 할 게 있어.

## 참조 셋 설계 원칙 (이걸 어기면 CSD가 스타일이 아니라 콘텐츠를 잼)

CSD는 "참조와 얼마나 같은 화풍인가"를 재는데, **참조 셋의 콘텐츠가 편향되면 점수가 오염돼**. 앞서 말한 content leakage 문제가 여기서 그대로 나와.

- **스타일은 균일하게, 콘텐츠는 최대한 다양하게.** 참조 30장이 전부 "사람 캐릭터"면, 교사가 "화산 단면도"를 뽑았을 때 스타일이 완벽해도 CSD가 낮게 나와. 인물/동물/사물/풍경/도형/과학 다이어그램을 골고루 섞어야 해.
- **20~50장이면 충분.** 더 늘려도 이득이 별로 없고, 스타일 균일성만 깨져.
- **네 30-keyword 스윕의 주제 분포와 참조 셋의 주제 분포를 맞춰**. 그래야 콘텐츠 편향이 상쇄돼.

## 조달 경로 두 개 — 사실 ②를 추천해

**② 자체 생성 golden set (추천)**
스윕 결과 중에 "이게 딱 우리가 원하는 스타일이다" 싶은 것들을 사람이 손으로 골라서 참조 셋으로 쓰는 방법. 장점이 큼:
- **라이선스 리스크 0.** 공교육 교사 대상 서비스라 이게 중요해. 스톡 이미지를 참조로 쓰면 평가 단계라 직접 배포는 아니어도, 나중에 LoRA 학습으로 넘어갈 때 그대로 문제가 돼.
- **타깃이 정확함.** 웹에서 긁은 "남의 플랫 일러스트"가 아니라 "우리 모델이 도달해야 할 지점"이 기준이 됨.
- 이미 30-keyword 스윕이 돌아가고 있으니 콘텐츠 다양성도 자동 확보돼.

방법: 스윕 출력 중 스타일 합격점인 것만 골라 `refs/` 에 모으고, CSD는 이 셋 대비로 계산. 리뷰 앱에 "golden set에 추가" 버튼 하나 붙이면 끝. 초기에 20장 모이면 바로 시작 가능하고, 좋은 게 나올 때마다 갱신하면 돼.

**① 웹/스톡 검색 (부트스트랩용)**
golden set이 아직 0장이라 시드가 필요하면 이걸로 시작하되, 위 라이선스 이슈 때문에 **평가 전용으로만** 쓰고 학습엔 안 쓰는 걸 명확히 해. 소스는 라이선스가 깨끗한 데로:
- **unDraw, Storyset, Open Doodles, Humaaans** — 무료·상업이용 가능한 플랫 일러스트 셋. 특히 unDraw는 스타일이 극도로 균일해서 참조 셋으로 이상적.
- **Wikimedia Commons** (CC 라이선스 필터)
- Freepik/Vecteezy는 라이선스 조건 확인 필요

## 검색 키워드

영어로 검색해야 결과가 훨씬 좋아. 스타일 축과 콘텐츠 축을 조합해:

**스타일 축 (핵심어)**
```
flat illustration
flat vector illustration
flat design illustration
minimal flat illustration
flat style infographic
educational flat illustration
children's textbook illustration
flat lay illustration no gradient
2D flat cartoon illustration
```

**콘텐츠 축 (다양성 확보용 — 스타일어와 곱해서)**
```
... science diagram / anatomy / plant / animal
... landscape / city / classroom
... people characters / student teacher
... objects tools / food
... geometry shapes / math
... history scene / map
```

예: `flat vector illustration science diagram`, `educational flat illustration plant life cycle`, `flat design illustration classroom students`

**제외 키워드** (섞이면 스타일 균일성 깨짐)
`3D`, `isometric`, `gradient`, `realistic`, `watercolor`, `line art` (라인아트는 채색 없는 거라 네 타깃이랑 다름), `shading`

---

**정리하자면**: 시드 20장은 unDraw/Storyset에서 콘텐츠 다양하게 골라 시작하고, 스윕 결과 중 합격작이 쌓이면 점진적으로 golden set으로 교체하는 게 제일 안전하고 정확해.

시드 셋이 준비되면 `score_image(image, prompt, ref_set) → {vqascore, csd, flatness, harmonic}` 모듈 짜줄게. 지금 바로 짤까, 아니면 참조 셋부터 모으고 올래?
</claude>