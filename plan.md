# 교육용 T2I 이미지 생성 서비스 개발 계획

> `~/Downloads/t2i-edu-dev-plan.md`에서 복사. Distill(Phase 4.5, DMD 증류)은 base 모델 e2e 생성이 먼저 검증된 뒤 시도할 계획이라 맨 후순위로 이동(Phase 8로 재배치).

## 전제 조건 (확정 사항)
- 용도: 공교육 교사의 수업용 프레젠테이션 삽화 생성
- 서빙: 16GB VRAM 이내, 자체 서빙 (API 사용 없음)
- 학습: 3090 (24GB) 서버에서 파인튜닝
- 스타일: 2~3개 프리셋 제공
- 타겟 언어: **스페인어 / 영어** (1차 목표) — 해당 언어 프롬프트에 잘 반응하는 모델을 유연하게 탐색
- 이미지 내 텍스트: 필수 아님 → **정책상 "이미지에는 텍스트 생성 안 함, 라벨은 PPT에서 추가"로 시작** (별도 트랙으로 후순위 검토)
- 최종 추론: **DMD 계열 one-step(또는 few-step) 증류 모델로 서빙**, teacher 모델은 학습·개발용

## 전체 로드맵 개요

```
Phase 0  요구사항 정의 + 평가 벤치마크 설계   (1주)
Phase 1  베이스 모델 선정                    (1~2주)
Phase 2  프롬프트 엔지니어링 + 프리셋 v0     (Phase 1과 병행)
Phase 3  스타일별 데이터셋 구축              (2~3주)
Phase 4  LoRA 파인튜닝                       (2~3주)
Phase 5  평가 및 품질 게이트                 (Phase 4와 병행)
Phase 6  서빙 시스템 구축                    (2주)
Phase 7  파일럿 및 반복 개선                 (지속)
Phase 8  DMD 증류 (one-step)                 (2~4주, 최대 기술 리스크 — base e2e 검증 후 후순위 시도)
```

핵심 원칙: **Phase 0에서 만든 고정 벤치마크 프롬프트셋이 이후 모든 단계의 통과 기준이 된다.** 이게 없으면 "좋아진 것 같다" 수준의 감으로 개발하게 됨.

---

## Phase 0. 요구사항 정의 + 평가 벤치마크 설계

- [ ] **Task 0-1. 사용 시나리오 정의**
  - **설명**: 어떤 교사가(초/중/고, 과목), 어떤 종류의 그림을(인물·장면 / 사물·개체 / 개념 도식 / 배경) 얼마나 자주 필요로 하는지 정리. 실제 교과서/수업 PPT 몇 개를 보면서 삽화 유형을 분류.
  - **목표**: 삽화 유형 분류표 (예: 과학-동식물, 사회-역사 장면, 국어-동화 삽화, 수학-생활 소재 등)
  - **통과 기준**: 유형 카테고리 5~10개 확정, 각 카테고리별 실제 수업 예시 2개 이상 첨부

- [ ] **Task 0-2. 스타일 프리셋 2~3개 정의**
  - **설명**: 각 프리셋의 시각 언어를 문서화. 후보 예시:
    - **플랫 일러스트**: 굵은 외곽선, 단색 면, 파스텔 톤 — PPT 삽화의 표준
    - **동화/수채화풍**: 저학년용, 부드러운 질감
    - **선화(색칠공부)**: 활동지용 — 기존 PixArt-Sigma 작업 재활용 가능
  - **목표**: 프리셋별 스타일 정의서 + 레퍼런스 이미지 10~20장 수집 (라이선스 출처 기록 필수)
  - **통과 기준**: 제3자(멘토 등)가 레퍼런스만 보고 세 프리셋을 구분할 수 있을 것

- [ ] **Task 0-3. 벤치마크 프롬프트셋 v1 구축**
  - **설명**: Task 0-1의 카테고리를 커버하는 평가용 프롬프트 50~100개 작성. 쉬운 것(사과 하나)부터 어려운 것(교실에서 실험하는 학생 3명, 복수 객체+행동)까지 난이도 분포 포함.
  - **목표**: 카테고리 × 난이도 매트릭스를 채우는 고정 프롬프트셋
  - **통과 기준**: 프롬프트셋 버전 고정(v1), 이후 모델 비교에 동일 세트+동일 시드 사용

- [ ] **Task 0-4. 평가 루브릭 정의**
  - **설명**: 사람이 채점할 항목 정의. 예: ① 프롬프트 충실도 ② 스타일 적합성 ③ 구조 오류(손가락, 신체 왜곡 등) ④ 교육 적합성(부적절/문화적 오류 없음) ⑤ 텍스트 아티팩트 없음. 각 1~5점.
  - **통과 기준**: 루브릭으로 두 사람이 같은 이미지를 채점했을 때 점수 차가 크지 않을 것 (기준이 모호하면 재작성)

---

## Phase 1. 베이스 모델 선정

- [ ] **Task 1-1. 후보 모델 및 라이선스 스크리닝**
  - **설명**: ① 16GB 서빙 ② 24GB LoRA 학습 ③ **상업/공공 서비스 가능 라이선스** ④ **공개 one-step 증류 레시피/체크포인트 존재 여부** 네 조건으로 필터링.
  - **주의 — 증류는 라이선스를 우회하지 못한다**: student는 teacher 가중치를 사용해 학습되므로 라이선스 조항상 파생물로 취급되는 게 일반적이고, FLUX.1 dev처럼 "output으로 경쟁 모델 학습/증류 금지"를 명시한 라이선스도 있다. 최종 서빙 모델이 증류본이어도 teacher 라이선스 스크리닝은 그대로 필요.

  | 모델 | 크기 | 16GB 서빙 | 24GB LoRA 학습 | 라이선스 | 비고 |
  |---|---|---|---|---|---|
  | SDXL | 3.5B UNet | 여유 | 매우 성숙 (kohya 등) | OpenRAIL++ (서비스 OK) | **DMD2 공식 코드+체크포인트 존재** — 증류 목표에 가장 유리 |
  | PixArt-Sigma | 0.6B DiT | 여유 (T5는 오프로드) | 가능, 도구 제한적 | 오픈 | 기존 경험 있음, 공개 one-step 레시피 없음 (LCM 정도) |
  | SD3.5 Medium | 2.5B | 가능 | 가능 | Community License (조건 확인) | 공개 DMD 레시피 없음 |
  | Sana | 0.6~1.6B | 매우 여유, 빠름 | 가능 | 확인 필요 | **SANA-Sprint(공식 one-step 증류판) 존재**, 저사양 서빙에 유리 |
  | FLUX.1 dev | 12B | 양자화해야 빠듯 | 24GB에서 빡빡 | **비상업 + 증류 금지 조항 — 서비스 불가** | 증류해도 탈락 |
  | FLUX.1 schnell | 12B | 양자화 필요 | distill 모델이라 파인튜닝 비추 | Apache 2.0 | |

  - **통과 기준**: 실측 대상 2~3개로 압축, 각각 라이선스 조항 원문 확인 기록

- [ ] **Task 1-2. 후보 모델 실측 비교**
  - **설명**: 벤치마크 프롬프트셋 v1을 동일 시드로 각 후보에 돌리고, 루브릭 채점 + VRAM/latency 실측 (서빙 목표 GPU 기준 fp16/bf16).
  - **목표**: 모델별 점수표 + 자원 사용량 표
  - **통과 기준**:
    - 1024px 1장 생성 시 VRAM peak < 14GB (2GB 여유)
    - 1장 생성 < 20초 (임시 목표, Phase 6에서 재조정)
    - 루브릭 총점 기준 최종 1개 선정, 트레이드오프 문서화

- [ ] **Task 1-3. 스페인어/영어 프롬프트 적합성 확인**
  - **설명**: 후보 모델들이 스페인어 프롬프트를 영어만큼 잘 이해하는지 확인 (텍스트 인코더가 영어 중심으로 학습된 경우가 많아 스페인어 성능이 떨어질 수 있음). 동일 의미의 스페인어/영어 프롬프트 쌍으로 벤치마크 실행 후 결과 비교. 격차가 크면 로컬 LLM 번역+프롬프트 확장 레이어(스페인어→영어) 도입을 고려.
  - **통과 기준**: 스페인어 입력 20개 → (필요 시 확장된 영어 프롬프트) → 생성 결과가 동등한 영어 프롬프트 대비 품질 동등

- [ ] **Task 1-4. DMD 학습 실행 가능성 스파이크 (조기 리스크 제거)**
  - **설명**: 최종 목표가 DMD one-step이므로, 선정 모델로 3090 24GB에서 DMD 학습 loop가 도는지 **최대한 일찍** 확인. DMD 학습은 generator + frozen teacher(real score) + fake score 세 벌이 메모리에 올라가는 구조라, 공식 셋업(다중 A100 기준)을 단일 24GB로 옮기려면 generator를 LoRA로 두기, gradient checkpointing, 512px 저해상도 선행 등의 다이어트가 필요하다.
  - **목표**: 짧은 스모크 학습(수백 step)으로 OOM 여부·step당 시간 실측
  - **통과 기준**: 24GB에서 학습 완주 가능 판정 or 불가 시 fallback(공개 증류 체크포인트 활용, Task 8-1의 경로 (c)) 조기 확정. **이 스파이크 결과가 모델 선정에 역으로 반영될 수 있음** — 예: 자체 증류가 불가하면 공식 증류판이 존재하는 모델(SDXL/Sana)이 사실상 강제됨.

---

## Phase 2. 프롬프트 엔지니어링 + 스타일 프리셋 v0 (Phase 1과 병행)

- [ ] **Task 2-1. 프리셋별 프롬프트 템플릿 작성**
  - **설명**: 선정 모델에서 각 스타일 프리셋을 프롬프트만으로 얼마나 재현 가능한지 실험. 스타일 prefix/suffix + negative prompt(text, watermark, letters, signature 등) 템플릿화.
  - **주의**: one-step 증류 모델은 CFG가 동작하지 않아 **negative prompt도 최종 모델에서는 못 쓴다** (guidance가 증류 과정에 흡수됨). negative prompt는 teacher 단계 전용 도구로 취급하고, 최종적으로는 positive prompt(+파인튜닝 데이터)만으로 원치 않는 요소가 억제되는 방향으로 설계할 것.
  - **목표**: 프리셋별 템플릿 v0
  - **통과 기준**: 벤치마크 셋에서 프리셋별 "스타일 적합성" 평균 점수 기록 → **이 점수가 파인튜닝의 베이스라인이자, 파인튜닝이 필요한 프리셋을 판별하는 근거**

- [ ] **Task 2-2. 파인튜닝 범위 결정**
  - **설명**: 프롬프트만으로 4점 이상 나오는 프리셋은 파인튜닝 생략 가능. 스타일 일관성이 흔들리는 프리셋만 LoRA 대상으로 지정.
  - **통과 기준**: 프리셋별 "프롬프트만 / LoRA 필요" 판정 완료

---

## Phase 3. 스타일별 데이터셋 구축 (LoRA 대상 프리셋만)

- [ ] **Task 3-1. 데이터 소싱 전략 수립**
  - **설명**: 스타일당 목표 200~500장 (스타일 LoRA는 소량 고품질이 대량 저품질보다 낫다). 소스 옵션:
    - CC0/퍼블릭 도메인 일러스트 (openclipart, 국립중앙박물관 공공누리 등)
    - 일러스트레이터 외주/구매 (소량 고품질 시드로 가장 확실)
    - 대형 모델로 합성 후 큐레이션 — **사용하는 모델의 output 사용 조항 반드시 확인** (예: dev 계열 output 상업 이용 제한)
  - **통과 기준**: 소스별 라이선스 근거 문서화. 공교육 서비스라 저작권 이슈 터지면 프로젝트가 죽는 지점이므로 여기서 타협 금지.

- [ ] **Task 3-2. 캡셔닝 파이프라인**
  - **설명**: VLM(Florence-2, Qwen2.5-VL 등)으로 자동 캡션 생성 + 스타일 트리거 토큰 부여 (예: `eduflat style`). 캡션에는 스타일 묘사를 빼고 내용만 남기는 게 스타일-내용 분리에 유리.
  - **통과 기준**: 샘플 50장 캡션 수동 검수, 오류율 < 10%

- [ ] **Task 3-3. 큐레이션 및 버저닝**
  - **설명**: 기존에 만든 파일시스템 버저닝 + Streamlit 리뷰 앱 워크플로우를 데이터셋 검수용으로 재활용. 승인/반려/사유 기록.
  - **통과 기준**: 데이터셋 v1 고정 (스타일당 200장 이상, 전수 검수 완료)

---

## Phase 4. LoRA 파인튜닝 (3090 서버)

- [ ] **Task 4-1. 학습 파이프라인 구축**
  - **설명**: 선정 모델에 맞는 도구 선택 (SDXL이면 kohya-ss가 표준, ai-toolkit/SimpleTuner도 후보 — 이미 조사해둔 것 활용). 스타일당 LoRA 1개, rank 16~32부터 시작.
  - **통과 기준**: 소량 데이터(50장)로 스모크 테스트 학습 1회 완주, 체크포인트 로드 및 추론 확인

- [ ] **Task 4-2. 본 학습 + 체크포인트 평가**
  - **설명**: LR sweep (1e-4 근방), 500~3000 step 사이 여러 체크포인트 저장. 각 체크포인트를 **학습에 안 쓴** 벤치마크 프롬프트로 평가.
  - **주의할 것**:
    - 과적합 징후: 프롬프트 다양성 무시하고 학습 이미지 구도 복제, 인물이 다 비슷해짐
    - 스타일은 잡혔는데 프롬프트 충실도가 떨어지면 step을 줄이거나 rank를 낮출 것
  - **통과 기준**: 프리셋별로 Task 2-1 베이스라인 대비 스타일 적합성 +1점 이상 상승 & 프롬프트 충실도 하락 없음

- [ ] **Task 4-3. LoRA 조합 테스트**
  - **설명**: 서빙 시 프리셋 전환 = LoRA 교체. 로딩 시간, 동일 시드 재현성 확인.
  - **통과 기준**: LoRA 스왑 < 3초, 프리셋 간 스타일 누출 없음

---

## Phase 5. 평가 및 품질 게이트

- [ ] **Task 5-1. 최종 품질 게이트**
  - **설명**: 벤치마크 전체를 최종 구성(teacher 모델 + 프롬프트 처리 레이어 + LoRA)으로 실행, 루브릭 채점. (Distill 이후에는 실제 서빙될 student 기준으로 재게이트 — Task 8-2 참고.)
  - **통과 기준 (예시, 조정 가능)**:
    - "그대로 수업에 쓸 수 있다" 판정 비율 ≥ 60% (4장 생성 중 1장 선택 기준이면 체감상 충분히 높음)
    - 심각한 구조 오류(신체 왜곡 등) 비율 < 10%
    - 부적절 콘텐츠 0건

- [ ] **Task 5-2. 안전성 검증**
  - **설명**: 교육용·아동 노출 콘텐츠이므로 필수. ① NSFW 필터(생성 후 이미지 분류기) ② 입력 프롬프트 필터(프롬프트 처리 레이어에서 겸임) ③ 악의적/장난 입력 시나리오 테스트.
  - **통과 기준**: 부적절 입력 테스트셋에 대해 필터 통과 0건

---

## Phase 6. 서빙 시스템 (16GB, teacher 모델 기준으로 먼저 구축)

- [ ] **Task 6-1. 추론 서버 구축**
  - **설명**: diffusers + FastAPI (직접 제어, 커스텀 용이) 또는 ComfyUI API 모드 중 택 1. 구성 요소:
    - 요청 큐 (GPU 1개 = 순차 처리, 동시 요청은 대기열)
    - 프리셋 → LoRA 매핑
    - (필요 시) 스페인어→영어 프롬프트 처리 레이어 (소형 LLM — **이 LLM의 VRAM도 16GB 예산에 포함됨**, CPU 추론 또는 별도 처리 고려. Task 1-3 결과에 따라 생략 가능)
    - 생성 후 NSFW 필터
  - **통과 기준**: VRAM peak < 15GB(전체 스택 합산), p95 latency는 teacher 모델 기준으로 우선 실측 (one-step 전환 후 Task 8-2에서 재측정), 1시간 연속 부하에서 메모리 누수 없음

- [ ] **Task 6-2. 최적화**
  - **설명**: 필요 시 적용 — fp16/bf16 기본, VAE tiling, torch.compile, (PixArt 계열이면) T5 인코더 CPU 오프로드. 병목은 텍스트 인코더, VAE 디코드, (사용 시) 프롬프트 처리 LLM일 가능성이 높으므로 end-to-end 프로파일링 기준으로 최적화 대상을 정할 것.
  - **통과 기준**: 품질 게이트 점수 하락 없이 latency 개선 확인 (최적화 전후 벤치마크 비교)

- [ ] **Task 6-3. 교사용 인터페이스 (최소 버전)**
  - **설명**: 입력창 + 프리셋 선택 + 4장 생성 + 다운로드. 처음엔 Streamlit/Gradio로 충분, 정식 UI는 파일럿 후.
  - **통과 기준**: 비개발자(교사 1명)가 설명 없이 이미지 1장을 뽑아 PPT에 붙일 수 있음

---

## Phase 7. 파일럿 및 반복 (teacher 모델 기준)

- [ ] **Task 7-1. 교사 파일럿**
  - **설명**: 교사 3~5명에게 2주 사용 → 실제 입력 프롬프트 로그 + 채택/버림 데이터 수집. **실사용 프롬프트가 벤치마크 v2의 재료가 된다.**
  - **통과 기준**: 파일럿 사용자 로그 확보, 실패 사례 카테고리화

- [ ] **Task 7-2. 반복 개선 루프**
  - **설명**: 실패 사례 → 데이터 보강 or 프롬프트 레이어 개선 → LoRA v2 → 벤치마크 재평가. 이후는 이 루프의 반복.

---

## Phase 8. DMD 증류 (one-step) — 후순위, base e2e 검증 완료 후 시도

> 이 계획에서 기술 리스크가 가장 큰 단계. Task 1-4 스파이크 결과에 따라 경로를 선택한다. **base 모델(teacher)로 e2e 생성이 문제 없이 도는 것을 먼저 확인한 뒤에만 착수.**

- [ ] **Task 8-1. 증류 구조 결정**
  - **설명**: 스타일이 프리셋별 LoRA로 존재하므로 세 가지 경로가 있다:
    - **(a) 프리셋별 merge 후 각각 증류**: 품질 최상, 비용 × 프리셋 수 (증류를 2~3번)
    - **(b) base 1회 증류 → student 위에 스타일 LoRA 적용**: 비용 최소. 단, teacher 기준으로 학습된 LoRA가 one-step student에서 스타일을 유지하는지 검증 필요 (커뮤니티 경험상 대체로 동작하나 품질 변동 있음)
    - **(c) 공개 증류 체크포인트(DMD2-SDXL, SANA-Sprint 등) + 스타일 LoRA**: 증류 학습 자체를 생략, 가장 빠르고 싼 경로
  - **목표**: (b)/(c)의 LoRA 호환성을 벤치마크로 실측 후 경로 확정
  - **통과 기준**: 경로 확정 문서화. **(c)가 품질 게이트를 통과하면 Task 8-2는 통째로 생략** — 자체 증류는 (c)가 부족할 때만 정당화된다.

- [ ] **Task 8-2. DMD 학습 (경로 (a)/(b) 선택 시)**
  - **설명**: Task 1-4에서 검증한 다이어트 구성(LoRA generator, gradient checkpointing, 512px→1024px 순차)으로 본 학습. DMD2 계열 레시피 기준, teacher가 만든 (noise, image) 쌍 준비 단계도 포함.
  - **통과 기준**:
    - 학습 완주 + student 벤치마크 점수가 teacher 대비 루브릭 평균 −0.3 이내
    - 1-step 1024px 생성 latency 실측 기록

- [ ] **Task 8-3. Few-step fallback**
  - **설명**: 1-step 품질이 게이트를 못 넘으면 4-step(DMD2 4-step, Lightning/Hyper-SD 계열)으로 타협. 16GB에서 4-step도 수 초 이내라 서비스 체감 차이는 작다.
  - **통과 기준**: 품질 게이트를 통과하는 최소 step 수 확정

- [ ] **Task 8-4. Student 기준 재게이트 및 서빙 전환**
  - **설명**: Task 5-1 품질 게이트와 Task 6-1 서빙 스택을 student(증류 모델) 기준으로 재실행. one-step/few-step은 CFG가 없으므로 negative prompt 제거, latency 목표(p95 < 3초 e2e) 재검증.
  - **통과 기준**: student 기준으로 Phase 5·6의 모든 통과 기준 재충족

---

## 별도 트랙 / 추가 고려사항

1. **이미지 내 텍스트**: 현재 오픈소스로 16GB에서 서비스 수준의 텍스트 렌더링 품질을 내는 모델은 드묾. 정책상 "텍스트 없는 이미지 + PPT 라벨"로 가고, 필요해지면 후처리(빈 라벨 박스 생성 → 프로그래매틱 텍스트 삽입) 방식을 먼저 검토할 것. 모델로 해결하려는 접근은 후순위.
2. **저작권/라이선스 3중 체크**: ① 베이스 모델 라이선스 ② 학습 데이터 라이선스 ③ 생성물 사용 권리. 공공 서비스라 셋 다 문서화해둘 것. **증류는 라이선스를 무효화하지 않음** — student는 teacher의 파생물로 취급되는 게 일반적이며, 라이선스에 따라 증류 자체를 금지하는 조항(FLUX.1 dev 등)도 있다.
3. **재현성**: 모든 실험은 시드 고정 + 설정 기록. 기존 파일시스템 버저닝 워크플로우를 실험 관리까지 확장 권장.
4. **성능 여유**: "16GB 한도"면 실제 목표는 14~15GB. OS/드라이버/파편화 여유분 필수.
